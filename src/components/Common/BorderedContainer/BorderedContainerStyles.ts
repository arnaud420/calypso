import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderWidth: 2,
    borderColor: '#c4c4c4',
    borderRadius: 5,
    marginTop: 10,
    marginBottom: 10,
    padding: 10,
  },
})

export default styles
