import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    height: '100%',
    alignItems: 'center',
    width: '100%',
    paddingTop: 20,
    paddingBottom: 20,
  },
  tabIcon: {
    height: 24,
    marginVertical: 2,
    width: 24,
  },
})

export default styles
